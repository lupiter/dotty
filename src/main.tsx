import React from 'react'
import ReactDOM from 'react-dom/client'
import Dotty from './Dotty.tsx'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <Dotty />
  </React.StrictMode>,
)
